import React, { JSX } from "react";
interface DatabaseProviderProps {
    children: React.ReactNode;
}
export declare const DatabaseProvider: ({ children, }: DatabaseProviderProps) => JSX.Element;
export {};
